@model Organizer_Domain.EntityModel.TaskPriority

@{
    ViewBag.Title = "Групи важливості";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Групи важливості</h3>

@using (Html.BeginForm("UpdateTaskPriority", "TaskPriority", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <div class="form-group">
            @Html.LabelFor(m => m.Name, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </div>
        </div>

        @Html.HiddenFor(m => m.TaskPriorityId)
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="Додати" />
            </div>
        </div>
    </fieldset>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}